#include <iostream>
#include <stdio.h>

using namespace std;

int main() {
    long long a, b;
    cin >> a >> b;
    if(a > b) {
        cout << 1;
    } else {
        cout << 0;
    }
    return 0;
}
